<style>
    /* 設定全局字體與背景 */
    body {
        font-family: 'Arial', sans-serif;
        background-color: #f4f4f9;
        margin: 0;
        padding: 0;
        color: #333;
    }

    /* 頂部導覽列 */
    header {
        background-color: #000000;
        padding: 15px 0;
    }

    .nav-container {
        width: 90%;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between; /* 使logo與導航項目分開 */
    }

    /* 讓 logo 貼齊左邊 */
    .logo {
        height: 50px;
        margin-left: 0; /* 移除預設邊距 */
    }

    nav {
        display: flex;
        justify-content: flex-end; /* 保持導航按鈕靠右對齊 */
        align-items: center;
    }

    nav a {
        color: #fff;
        text-decoration: none;
        margin: 0 15px;
        font-size: 16px;
        text-transform: uppercase;
    }

    nav a:hover {
        color: #ff8c00;
    }

    .dropdown {
        position: relative;
        display: inline-block;
    }

    .dropdown-btn {
        background-color: #2a3d4f;
        color: white;
        border: none;
        padding: 10px 20px;
        cursor: pointer;
        font-size: 16px;
        transition: background-color 0.3s;
    }

    .dropdown-btn:hover {
        background-color: #ff8c00;
    }

    .dropdown-content {
        display: none;
        position: absolute;
        background-color: #f4f4f9;
        min-width: 160px;
        z-index: 1;
        top: 100%;
        left: 0;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    }

    .dropdown-content a {
        color: #333;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
    }

    .dropdown-content a:hover {
        background-color: #ddd;
    }

    .dropdown:hover .dropdown-content {
        display: block;
    }

    /* 主要內容區域 */
    main {
        text-align: center;
        padding: 50px 20px;
        background-color: white;
        margin: 20px 0;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    h1 {
        color: #2a3d4f;
        font-size: 36px;
        margin-bottom: 10px;
    }

    p {
        font-size: 18px;
        color: #555;
    }

    /* 頁尾 */
    footer {
        background-color: #2a3d4f;
        color: #fff;
        text-align: center;
        padding: 10px;
        position: fixed;
        width: 100%;
        bottom: 0;
    }

    /* 響應式設計 */
    @media (max-width: 768px) {
        .nav-container {
            flex-direction: column;
            align-items: center;
        }

        nav a {
            margin: 10px 0;
        }

        .dropdown-content {
            min-width: 100%;
        }

        main {
            padding: 30px 20px;
        }
    }

    .typing {
        display: inline-block;
        font-size: 18px;
        color: #555;
        white-space: nowrap;
        overflow: hidden;
        border-right: 3px solid #333;
        width: 0;
        animation: typing 5s steps(40) 1s forwards, blinkCaret 0.75s step-end infinite;
    }

    /* 打字動畫設定 */
    @keyframes typing {
        from {
            width: 0;
        }
        to {
            width: 100%;
        }
    }

    /* 光標閃爍效果 */
    @keyframes blinkCaret {
        50% {
            border-color: transparent;
        }
    }
</style>
<header>
    <div class="nav-container">
        <img src="/images/NEW LOGO 縮小.png" alt="公司 LOGO" class="logo">
        <nav>
            <a href="/">首頁</a>
            <a href="/announcements">公告</a>
            <a href="/nofunction">產品</a>
            <a href="/travel">金融旅遊</a>
            <a href="/youtube">YouTube Premium 使用須知</a>
            <a href="https://sites.google.com/view/chengxuntvcx" target="_blank">舊版網頁</a>
            <div class="dropdown">
                <button class="dropdown-btn">更多服務</button>
                <div class="dropdown-content">
                    <a href="/nofunction">旅遊證申辦</a>
                    <a href="/nofunction">雲端儲存</a>
                    <a href="https://sites.google.com/view/chengxuntvcx" target="_blank">舊版網頁</a>
                </div>
            </div>
            <a href="/login">員工登入</a>
        </nav>
    </div>
</header>
